{# Jinja2 template for NanoPB methods documentation generation #}
# NanoPB Methods: {{ name }}

> **This file is auto-generated by embgen.** Do not edit manually.

## Summary

| Property | Value |
|----------|-------|
| API Name | {{ name }} |
| Total Methods | {{ methods|length }} |
| Max Commands Per Packet | {{ max_count }} |
| Generated On | {{ generated_on }} |

## Table of Contents

| Name | Description |
|------|-------------|
{% for method in methods %}
| [{{ method.name }}](#method-{{ method.name | lower | replace(' ', '-') }}) | {{ method.description or 'No description' }} |
{% endfor %}

---
{% for method in methods %}

## Method: {{ method.name }}

{% if method.description %}
{{ method.description }}
{% endif %}

| Property | Value |
|----------|-------|
| Name | {{ method.name }} |
| Arguments | {{ method.args|length if method.args else 0 }} |
| Returns | {{ method.returns|length if method.returns else 0 }} |
{% if method.args %}

### Arguments

| Name | Type | Default | Description |
|------|------|---------|-------------|
{% for arg in method.args %}
| `{{ arg.name|lower }}` | `{{ arg.type_python }}` | `{{ arg.default.name if arg.default is defined and arg.default.name is defined else arg.default if arg.default != None else 'None' }}` | {{ arg.description or '-' }} |
{% endfor %}
{% for arg in method.args %}
{% if arg.enums %}

### Enum: {{ method.name }}.{{ arg.name }}

{{ arg.description or '' }}

| Value | Name | Description |
|-------|------|-------------|
{% for enum in arg.enums | sort(attribute='value') %}
| {{ enum.value }} (`0x{{ '%X' % enum.value }}`) | `{{ enum.name }}` | {{ enum.description or '-' }} |
{% endfor %}
{% endif %}
{% endfor %}
{% else %}

**Arguments:** None
{% endif %}
{% if method.returns %}

### Return Values

| Name | Type | Description |
|------|------|-------------|
{% for ret in method.returns %}
| `{{ ret.name|lower }}` | `{{ ret.type_python }}` | {{ ret.description or '-' }} |
{% endfor %}
{% for ret in method.returns %}
{% if ret.enums %}

### Return Enum: {{ method.name }}.{{ ret.name }}

{{ ret.description or '' }}

| Value | Name | Description |
|-------|------|-------------|
{% for enum in ret.enums | sort(attribute='value') %}
| {{ enum.value }} (`0x{{ '%X' % enum.value }}`) | `{{ enum.name }}` | {{ enum.description or '-' }} |
{% endfor %}
{% endif %}
{% endfor %}
{% else %}

**Returns:** None
{% endif %}
{% endfor %}

---

## Quick Reference

### All Methods at a Glance

| Method | Arguments | Returns |
|--------|-----------|---------|
{% for method in methods %}
| {{ method.name }} | {% for arg in method.args %}{{ arg.name }}{% if not loop.last %}, {% endif %}{% endfor %}{% if not method.args %}-{% endif %} | {% for ret in method.returns %}{{ ret.name }}{% if not loop.last %}, {% endif %}{% endfor %}{% if not method.returns %}-{% endif %} |
{% endfor %}
