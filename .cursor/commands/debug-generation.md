---
description: Debug missing or wrong embgen output
---

Follow `.cursor/skills/debug-generation/SKILL.md`. Start with domain detection, then Pydantic validation, then template discovery, then `render()` vs `post_generate()`. For registers `.hjson` / `regtool.py`, use `.cursor/skills/registers-regtool/SKILL.md`.
